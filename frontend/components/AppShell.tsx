'use client';import {ReactNode,useState} from 'react';import Link from 'next/link';import Sidebar from './Sidebar';import Topbar from './Topbar';import BottomNav from './BottomNav';export default function AppShell({children}:{children:ReactNode}){const [menu,setMenu]=useState(false);return <div className="min-h-screen flex"><Sidebar/><div className="flex-1 min-w-0"><Topbar onMenu={()=>setMenu(!menu)}/>{menu&&<div className="md:hidden fixed inset-0 z-50 bg-black/40" onClick={()=>setMenu(false)}><div className="w-72 h-full bg-[var(--surface)] p-5" onClick={e=>e.stopPropagation()}><div className="font-bold text-lg mb-6">FinSight</div>{['dashboard','transactions','budgets','subscriptions','insights','forecast','reports','assistant','settings'].map(x=><Link key={x} href={'/'+x} onClick={()=>setMenu(false)} className="block py-3 capitalize text-[var(--muted)] hover:text-[var(--text)]">{x}</Link>)}</div></div>}<main className="px-4 md:px-7 py-6 pb-24 md:pb-8 max-w-[1600px] mx-auto">{children}</main><BottomNav/></div></div>}
