'use client';import {Moon,Sun} from 'lucide-react';import {useEffect,useState} from 'react';export default function ThemeToggle(){const [dark,setDark]=useState(false);useEffect(()=>setDark(document.documentElement.classList.contains('dark')),[]);function toggle(){document.documentElement.classList.toggle('dark');setDark(!dark);localStorage.setItem('theme',!dark?'dark':'light')}return <button aria-label="Toggle theme" onClick={toggle} className="h-9 w-9 rounded-xl border border-[var(--border)] bg-[var(--surface)] grid place-items-center hover:-translate-y-0.5 transition">{dark?<Sun size={17}/>:<Moon size={17}/>}</button>}
