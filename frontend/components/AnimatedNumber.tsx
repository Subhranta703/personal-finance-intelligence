'use client';import {motion,useSpring,useTransform} from 'framer-motion';import {useEffect} from 'react';export default function AnimatedNumber({value,prefix='₹'}:{value:number;prefix?:string}){const spring=useSpring(0,{stiffness:70,damping:18});const rounded=useTransform(spring,v=>`${prefix}${Math.round(v).toLocaleString('en-IN')}`);useEffect(()=>spring.set(value),[value,spring]);return <motion.span>{rounded}</motion.span>}
