'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {api} from '../../lib/api';import {toast} from 'sonner';export default function Register(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);try{await api.post('/auth/register',{name,email,password});router.push('/dashboard')}catch(err:any){toast.error(err?.response?.data?.message||'Registration failed')}finally{setLoading(false)}}return <div className="min-h-screen grid lg:grid-cols-2"><div className="hidden lg:flex bg-gradient-to-br from-[#0B1220] via-[#111827] to-indigo-950 text-white p-12 flex-col justify-between"><div className="font-bold text-xl">◈ FinSight</div><div><p className="text-indigo-300 font-semibold text-sm">START WITH YOUR DATA</p><div className="mt-3 text-5xl font-black">Build a clearer<br/>financial picture.</div></div><div className="text-xs text-slate-500">Your demo data is synthetic and removable.</div></div><div className="flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-md"><Link href="/" className="text-sm text-[var(--muted)]">← Back</Link><h1 className="mt-10 text-3xl font-black">Create your account</h1><p className="mt-2 text-[var(--muted)]">Start exploring your financial patterns.</p>{[['Name',name,setName,'text'],['Email',email,setEmail,'email'],['Password',password,setPassword,'password']].map(([label,val,set,type])=><label key={label as string} className="block mt-5 text-sm font-semibold">{label as string}<input required type={type as string} value={val as string} onChange={e=>(set as any)(e.target.value)} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-4 py-3 outline-none focus:ring-2 focus:ring-indigo-500/30"/></label>)}<button disabled={loading} className="mt-6 w-full rounded-xl bg-indigo-500 py-3.5 text-white font-bold">{loading?'Creating…':'Create account'}</button><p className="mt-5 text-center text-sm text-[var(--muted)]">Already have an account? <Link href="/login" className="text-indigo-500 font-semibold">Sign in</Link></p></form></div></div>}
