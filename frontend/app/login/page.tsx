'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {api} from '../../lib/api';import {toast} from 'sonner';export default function Login(){const [email,setEmail]=useState('demo@finsight.app'),[password,setPassword]=useState('password123'),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);try{await api.post('/auth/login',{email,password});router.push('/dashboard')}catch(err:any){toast.error(err?.response?.data?.message||'Login failed')}finally{setLoading(false)}}return <div className="min-h-screen grid lg:grid-cols-2"><div className="hidden lg:flex bg-[#0B1220] text-white p-12 flex-col justify-between"><div className="font-bold text-xl">◈ FinSight</div><div><div className="text-5xl font-black tracking-tight">Your money,<br/><span className="text-cyan-400">decoded.</span></div><p className="mt-5 text-slate-400 max-w-md">A transparent finance intelligence workspace built with deterministic algorithms.</p></div><div className="text-xs text-slate-500">No paid AI dependency · MERN · TypeScript</div></div><div className="flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-md"><Link href="/" className="text-sm text-[var(--muted)]">← Back</Link><h1 className="mt-10 text-3xl font-black">Welcome back</h1><p className="mt-2 text-[var(--muted)]">Sign in to your FinSight workspace.</p><label className="block mt-8 text-sm font-semibold">Email<input value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-4 py-3 outline-none focus:ring-2 focus:ring-indigo-500/30"/></label><label className="block mt-4 text-sm font-semibold">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--surface)] px-4 py-3 outline-none focus:ring-2 focus:ring-indigo-500/30"/></label><button disabled={loading} className="mt-6 w-full rounded-xl bg-indigo-500 py-3.5 text-white font-bold disabled:opacity-60">{loading?'Signing in…':'Sign in'}</button><p className="mt-5 text-center text-sm text-[var(--muted)]">New here? <Link href="/register" className="text-indigo-500 font-semibold">Create account</Link></p></form></div></div>}
