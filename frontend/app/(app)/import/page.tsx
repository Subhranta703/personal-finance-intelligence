import FeaturePage from '../../../components/FeaturePage';export default function ImportPage(){return <FeaturePage eyebrow="Bring your data" title="Import statements" description="CSV import is supported by the backend pipeline; PDF processing is designed as a safe parser abstraction. Preview data before writing it to MongoDB."><div className="grid md:grid-cols-2 gap-5"><div className="card p-6"><div className="text-3xl">CSV</div><h3 className="mt-3 font-bold">Bank statement CSV</h3><p className="mt-2 text-sm text-[var(--muted)]">Detect date, description, merchant, debit and credit columns, then preview before import.</p><button className="mt-5 rounded-xl bg-indigo-500 text-white px-4 py-2.5 text-sm font-semibold">Choose CSV</button></div><div className="card p-6"><div className="text-3xl">PDF</div><h3 className="mt-3 font-bold">Statement PDF</h3><p className="mt-2 text-sm text-[var(--muted)]">The Node parser abstraction validates extractability instead of silently inventing transactions.</p><button className="mt-5 rounded-xl border border-[var(--border)] px-4 py-2.5 text-sm font-semibold">Choose PDF</button></div></div></FeaturePage>}
