'use client';import FeaturePage from '../../../components/FeaturePage';import {FadeIn} from '../../../components/Motion';export default function Budgets(){const data=[['Food',4100,5000,'amber'],['Shopping',2600,4000,'cyan'],['Transport',1700,3000,'emerald'],['Entertainment',900,2000,'indigo']];return <FeaturePage eyebrow="Guardrails" title="Budgets" description="Set category limits and see how much of your plan has been used."><div className="grid lg:grid-cols-2 gap-5">{data.map(([n,u,t,c],i)=><FadeIn key={n as string} delay={i*.05}><div className="card p-5"><div className="flex justify-between"><div><div className="font-bold">{n as string}</div><div className="text-xs text-[var(--muted)] mt-1">Monthly limit</div></div><div className="text-sm font-bold">₹{Number(u).toLocaleString()} / ₹{Number(t).toLocaleString()}</div></div><div className="mt-5 h-3 rounded-full bg-[var(--surface2)] overflow-hidden"><div className={`h-full rounded-full ${c==='amber'?'bg-amber-500':c==='cyan'?'bg-cyan-500':c==='emerald'?'bg-emerald-500':'bg-indigo-500'}`} style={{width:`${Number(u)/Number(t)*100}%`}}/></div><div className="mt-3 flex justify-between text-xs text-[var(--muted)]"><span>{Math.round(Number(u)/Number(t)*100)}% used</span><span>₹{(Number(t)-Number(u)).toLocaleString()} remaining</span></div></div></FadeIn>)}</div></FeaturePage>}
