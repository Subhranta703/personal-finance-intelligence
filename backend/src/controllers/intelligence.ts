import {Request,Response} from 'express';import {Transaction} from '../models/Transaction';import {anomalies,overview,budgetStatus} from '../services/analytics';import {detectRecurring} from '../algorithms/recurring';import {movingAverage,weightedMovingAverage} from '../utils/stats';import {parseIntent} from '../algorithms/assistant';
export async function getOverview(req:Request,res:Response){res.json(await overview(req.userId!))}
export async function getAnomalies(req:Request,res:Response){res.json(await anomalies(req.userId!))}
export async function getRecurring(req:Request,res:Response){const tx=await Transaction.find({userId:req.userId,type:'expense'}).lean();res.json(detectRecurring(tx.map(x=>({merchant:x.merchant,amount:x.amount,date:x.date}))))}
export async function getForecast(req:Request,res:Response){const tx=await Transaction.find({userId:req.userId,type:'expense'}).lean();const monthly=new Map<string,number>();for(const t of tx){const d=new Date(t.date),k=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;monthly.set(k,(monthly.get(k)||0)+t.amount)}const values=[...monthly.entries()].sort().slice(-6);const nums=values.map(x=>x[1]);res.json({history:values.map(([month,value])=>({month,value})),nextMonth:Math.round(movingAverage(nums)),weightedNext:Math.round(weightedMovingAverage(nums)),method:'Moving average / weighted moving average'})}
export async function getBudgets(req:Request,res:Response){const month=String(req.query.month||new Date().toISOString().slice(0,7));res.json(await budgetStatus(req.userId!,month))}
export async function assistant(req:Request,res:Response){const q=String(req.body.question||'');const p=parseIntent(q);const now=new Date(),start=new Date(now.getFullYear(),now.getMonth(),1);const tx=await Transaction.find({userId:req.userId,type:'expense',date:{$gte:start}}).lean();if(p.intent==='CATEGORY_SPENDING'&&p.category){const amount=tx.filter(t=>t.category.toLowerCase()===p.category!.toLowerCase()).reduce((s,t)=>s+t.amount,0);return res.json({answer:`You spent ₹${Math.round(amount).toLocaleString('en-IN')} on ${p.category} this month.`,intent:p})}if(p.intent==='LARGEST_EXPENSES'){const top=[...tx].sort((a,b)=>b.amount-a.amount).slice(0,5);return res.json({answer:top.length?`Your largest expense this month was ₹${top[0].amount.toLocaleString('en-IN')} at ${top[0].merchant}.`:'No expense data yet.',items:top})}return res.json({answer:'I can answer questions about category spending, largest expenses, subscriptions, month comparisons, and savings scenarios.',intent:p})}
