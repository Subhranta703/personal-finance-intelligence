export function parseIntent(q:string){const s=q.toLowerCase();let intent='OVERVIEW';if(/how much.*(food|shopping|transport|grocer|rent|subscription)/.test(s))intent='CATEGORY_SPENDING';else if(/largest|biggest/.test(s))intent='LARGEST_EXPENSES';else if(/compare|last month|previous month/.test(s))intent='COMPARE_MONTHS';else if(/subscription/.test(s))intent='SUBSCRIPTIONS';else if(/save|saving|reduce/.test(s))intent='WHAT_IF';return {intent,category:/food/.test(s)?'Food':/shopping/.test(s)?'Shopping':/transport/.test(s)?'Transport':/grocer/.test(s)?'Groceries':/rent/.test(s)?'Rent':undefined}}
